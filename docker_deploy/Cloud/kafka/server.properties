# KRaft Mode - Dynamic Configuration via Environment Variables
# These are set by docker-compose and override hardcoded values
node.id=${KAFKA_NODE_ID}
process.roles=${KAFKA_PROCESS_ROLES}
controller.quorum.voters=${KAFKA_CONTROLLER_QUORUM_VOTERS}

# Listeners - Dynamic
listeners=${KAFKA_LISTENERS}
advertised.listeners=${KAFKA_ADVERTISED_LISTENERS}
inter.broker.listener.name=${KAFKA_INTER_BROKER_LISTENER_NAME}
controller.listener.names=${KAFKA_CONTROLLER_LISTENER_NAMES}
listener.security.protocol.map=${KAFKA_LISTENER_SECURITY_PROTOCOL_MAP}

# Storage
log.dirs=/var/lib/kafka/data

# Cluster Configuration
log.retention.hours=${KAFKA_LOG_RETENTION_HOURS}
log.retention.bytes=${KAFKA_LOG_RETENTION_BYTES}
auto.create.topics.enable=${KAFKA_AUTO_CREATE_TOPICS_ENABLE}
default.replication.factor=${KAFKA_DEFAULT_REPLICATION_FACTOR}
min.insync.replicas=${KAFKA_MIN_INSYNC_REPLICAS}

# Topic Creation
num.partitions=3
auto.topic.creation.enable=true
auto.create.groups.enable=true

# Performance tuning
num.network.threads=8
num.io.threads=8
socket.send.buffer.bytes=102400
socket.receive.buffer.bytes=102400
delete.topic.enable=true
